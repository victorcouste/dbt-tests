local_postgres:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: victorcoustenoble
      pass: 
      port: 5432
      dbname: mydb
      schema: public
      threads: 1
      keepalives_idle: 0 # default 0, indicating the system default
